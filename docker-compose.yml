services:
  snow-asset-agent:
    build: .
    env_file:
      - .env
    stdin_open: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "from snow_asset_agent import __version__; print(__version__)"]
      interval: 30s
      timeout: 5s
      retries: 3
